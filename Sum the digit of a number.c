#include <stdio.h>
int main(){
    int i,a,b,c,d,num,sum=0;
    printf("Enter the number : ");
    scanf("%d",&num);
    if(num>100)
        {
            for(i=1;i<=n;i++)
            {
                sum=num/10;
                num=num%10;
                sum=sum+(num/10);
                num=num%10;

            }


        }

       else
        {
           sum=sum+(num/10);
           sum=sum+(num%10);
        }













return 0;
}
